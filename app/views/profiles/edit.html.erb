<div class="card">
	<h5 class="card-header">Editing Profile</h5>
	<div class="card-body">
		<%= render 'form', profile: @profile %>
	</div>
	<div class="card-footer text-muted">
		<%= link_to 'Show', @profile %> |
		<%= link_to 'Back', profiles_path %>
	</div>
</div>
